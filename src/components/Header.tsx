import React from 'react'; 

interface HeaderProperties {
    id: number;
    label: string;
}

const Header: React.FC <HeaderProperties> = ({id, label}) => {
    return (
        <div></div>
    );
};